#include "main.h"

/**
 * _strcmp - function that compare two strings.
 * @s1: string
 * @s2: string
 * Return: 0 if strings are equal
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
